<%= form_for([@movie, @location_linker]) do |f| %>
  <% if @location_linker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_linker.errors.count, "error") %> prohibited this location_linker from being saved:</h2>

      <ul>
      <% @location_linker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_area :location %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
