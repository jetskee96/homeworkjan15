
<div class="panel panel-default">
  <div class="panel-body">
    <div class="row">
      <div class="col-md-4">
         <%= image_tag @movie.poster.url(:medium) %>

        <div class="table-responsive">
          <table class="table">
            <tbody>
              <tr>
                <td><strong>Title:</strong></td>
                <td><%= @movie.title %></td>
              </tr>
              <tr>
                <td><strong>Description:</strong></td>
                <td><%= @movie.description %></td>
              </tr>
              <tr>
                <td><strong>Movie length:</strong></td>
                <td><%= @movie.movie_length %></td>
              </tr>
              <tr>
                <td><strong>User:</strong></td>
                <td><%= @movie.user.email %></td>
              </tr>
                  <tr>
            <td> <select class="form-control">
  <option></option>
  <option></option>
  <option></option>
  <option>4</option>
  <option>5</option>
         </select> <button type="submit" class="btn btn-default">Confirm identity</button>
             <%= link_to "Add new Genre", new_movie_genre_linker_path(@movie) %>
              <%= link_to "Add new cliche", new_movie_common_linker_path(@movie) %>
              <%= link_to "Add new theme", new_movie_theme_linker_path(@movie) %>
              <%= link_to "Add new location", new_movie_location_linker_path(@movie) %>

  </div>    
                <td></td>
                    <td></td>
                    <td></td>
              </tr>
               
               <tr>

              
    
               </tr>
            </tbody>
          </table>
            
        </div>
      </div>
  <%= render 'linker' %>


<%= link_to 'Edit', edit_movie_path(@movie) %> |


