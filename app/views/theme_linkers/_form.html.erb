<%= form_for([@movie, @theme_linker]) do |f| %>
  <% if @theme_linker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theme_linker.errors.count, "error") %> prohibited this theme_linker from being saved:</h2>

      <ul>
      <% @theme_linker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :theme %><br>
    <%= f.text_area :theme %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
