<%= form_for([@movie, @genre_linker]) do |f| %>
  <% if @genre_linker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@genre_linker.errors.count, "error") %> prohibited this genre_linker from being saved:</h2>

      <ul>
      <% @genre_linker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :genre %><br>
    <%= f.text_area :genre %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
