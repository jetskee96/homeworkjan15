<%= form_for([@movie, @common_linker]) do |f| %>
  <% if @common_linker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@common_linker.errors.count, "error") %> prohibited this common_linker from being saved:</h2>

      <ul>
      <% @common_linker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cliche %><br>
    <%= f.text_area :cliche %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
