<%= form_for([@movie, @movie_genre, @movie_genre_comment]) do |f| %>
  <% if @movie_genre_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie_genre_comment.errors.count, "error") %> prohibited this movie_genre_comment from being saved:</h2>

      <ul>
      <% @movie_genre_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
